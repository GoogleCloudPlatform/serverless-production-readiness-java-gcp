steps:
- name: maven:3-openjdk-17
  entrypoint: mvn
  args: ["spring-boot:build-image", "-P${_TYPE}"]
- name: gcr.io/cloud-builders/docker
  args: ["tag", "faulty-${_TYPE}", "gcr.io/$PROJECT_ID/faulty-${_TYPE}"]
images: ["gcr.io/$PROJECT_ID/faulty-${_TYPE}"]
